# .readthedocs.yml

version: 2

sphinx:
  configuration: docs/conf.py

build:
  os: ubuntu-lts-latest
  tools:
    "python": "3.13"
python:
  install:
    - method: uv
      command: sync
      groups:
        - docs
